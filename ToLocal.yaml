dest:
  local:
    registry: registry.local.y3-3am.top

# 全局指定
sync:
  - local
platform:
  - linux/amd64

sources:
  docker.io:
    images:
      cloudflare/cloudflared:
        platform:
          - linux/arm64
          - linux/amd64
      joxit/docker-registry-ui: main
      gdy666/lucky: v2
      louislam/uptime-kuma: 2

  ghcr.io:
    images:
      finb/bark-server:
        platform:
          - linux/arm64
          - linux/amd64
      usememos/memos: 0.29.1
      sagernet/sing-box: [ latest, latest-testing ]